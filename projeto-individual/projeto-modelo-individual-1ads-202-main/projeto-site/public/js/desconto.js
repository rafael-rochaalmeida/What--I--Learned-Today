function verificarRespCorinthians() {
   const  respostaUserCorinthians = Number(inp_corinthians.value);
    if (respostaUserCorinthians == 30) {
        // cupomCorinthians = parseInt(Math.random() * 900000000);
        container_sumir_corinthians.style.display = 'none';
        mensagem_acertou_corinthians.innerHTML = `Parabéns, você acertou!!. Seu cupom de desconto é o 5479ASSJ5`;
        console.log(cupomCorinthians);
    } else {
        alert('Resposta errada!. Tente novamente ;)')
    }
}


function verificarRespPalmeiras() {
    const respostaUser = Number(inp_palmeiras.value);
    if (respostaUser == 1914) {
        // const cupomPalmeiras = parseInt(Math.random() * 900000000);
        container_sumir_palmeiras.style.display = 'none';
        mensagem_acertou_palmeiras.innerHTML = `Parabéns, você acertou!!. Seu cupom de desconto é o 954a7Q424`;

    } else {
        alert('Resposta errada!. Tente novamente ;)')
    }
}


function verificarRespSaoPaulo() {
    const respostaUser = Number(inp_sao_paulo.value);
    if (respostaUser == 2005) {
        // const cupomSaoPaulo = parseInt(Math.random() * 900000000);
        container_sumir_sao_paulo.style.display = 'none';
        mensagem_acertou_sao_paulo.innerHTML = `Parabéns, você acertou!!. Seu cupom de desconto é o a8a8Q4217`;

    } else {
        alert('Resposta errada!. Tente novamente ;)')
    }
}


function verificarRespSantos() {
    const respostaUser = Number(inp_santos.value);
    if (respostaUser == 3) {
        // const cupomSantos = parseInt(Math.random() * 900000000);
        container_sumir_santos.style.display = 'none';
        mensagem_acertou_santos.innerHTML = `Parabéns, você acertou!!. Seu cupom de desconto é o 9ACEsa469`;

    } else {
        alert('Resposta errada!. Tente novamente ;)')
    }
}



